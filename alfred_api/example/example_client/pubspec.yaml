name: example_client
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.3.0-174.2.beta

# Add regular dependencies here.
dependencies:
  example_models:
    path: /Users/tom/alfred_api/alfred_api/example/example_models
  http: ^1.1.2
  # example:

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
